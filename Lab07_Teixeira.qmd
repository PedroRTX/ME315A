---
title: "Lab07_Teixeira"
author: "Pedro Rocha Teixeira - 253670"
format: pdf
editor: visual
---

```{r}
library(RSQLite)
library(DBI)
```

1.  Baixe o arquivo `uwmadison.sqlite3`. Conecte-se a ele usando o pacote `RSQLite`, armazenando a conexão em uma variável `conn`.

```{r}
conn = dbConnect(SQLite(),
'/cloud/project/database.sqlite3')
conn
```

2.  Quem são e quantos são os professores que lecionaram disciplinas cujo tópico era estatística (`subjects.abbreviation='STAT'`);

```{sql connection=conn}
SELECT DISTINCT i.id, i.name
FROM instructors i
JOIN teachings t ON i.id = t.instructor_id
JOIN sections sec ON t.section_uuid = sec.uuid
JOIN course_offerings co ON sec.course_offering_uuid = co.uuid
JOIN subject_memberships sm ON co.uuid = sm.course_offering_uuid
JOIN subjects s ON sm.subject_code = s.code
WHERE s.abbreviation = 'STAT'
```
```{sql connection=conn}
SELECT COUNT(DISTINCT i.id) AS quantidade_professores
FROM instructors i
JOIN teachings t ON i.id = t.instructor_id
JOIN sections sec ON t.section_uuid = sec.uuid
JOIN course_offerings co ON sec.course_offering_uuid = co.uuid
JOIN subject_memberships sm ON co.uuid = sm.course_offering_uuid
JOIN subjects s ON sm.subject_code = s.code
WHERE s.abbreviation = 'STAT'
```

3.  O GPA americano é definido numa escala de 0 a 4, em que A = 4, AB = 3.5, B = 3, BC = 2.5, C = 2, D = 1 e F = 0. Determinando a nota média de cada oferecimento pela ponderação da quantidade de alunos em cada extrato com os valores numéricos de cada conceito, indique (no que se referente a disciplinas no assunto de estatística):

- Quem é o professor mais difícil?

```{sql connection=conn}
SELECT 
    i.id,
    i.name,
    SUM(gd.a_count*4.0 + gd.ab_count*3.5 + gd.b_count*3.0 + gd.bc_count*2.5 + gd.c_count*2.0 + gd.d_count*1.0) * 1.0 / 
    SUM(gd.a_count + gd.ab_count + gd.b_count + gd.bc_count + gd.c_count + gd.d_count + gd.f_count) AS gpa_medio
FROM instructors i
JOIN teachings t ON i.id = t.instructor_id
JOIN sections sec ON t.section_uuid = sec.uuid
JOIN course_offerings co ON sec.course_offering_uuid = co.uuid
JOIN subject_memberships sm ON co.uuid = sm.course_offering_uuid
JOIN subjects s ON sm.subject_code = s.code
JOIN grade_distributions gd ON co.uuid = gd.course_offering_uuid
WHERE s.abbreviation = 'STAT'
GROUP BY i.id, i.name
HAVING SUM(gd.a_count + gd.ab_count + gd.b_count + gd.bc_count + gd.c_count + gd.d_count + gd.f_count) > 0
ORDER BY gpa_medio ASC
LIMIT 1;
```

- Quem é o professor mais fácil?

```{sql connection=conn}
SELECT 
    i.id,
    i.name,
    SUM(gd.a_count*4.0 + gd.ab_count*3.5 + gd.b_count*3.0 + gd.bc_count*2.5 + gd.c_count*2.0 + gd.d_count*1.0) * 1.0 / 
    SUM(gd.a_count + gd.ab_count + gd.b_count + gd.bc_count + gd.c_count + gd.d_count + gd.f_count) AS gpa_medio
FROM instructors i
JOIN teachings t ON i.id = t.instructor_id
JOIN sections sec ON t.section_uuid = sec.uuid
JOIN course_offerings co ON sec.course_offering_uuid = co.uuid
JOIN subject_memberships sm ON co.uuid = sm.course_offering_uuid
JOIN subjects s ON sm.subject_code = s.code
JOIN grade_distributions gd ON co.uuid = gd.course_offering_uuid
WHERE s.abbreviation = 'STAT'
GROUP BY i.id, i.name
HAVING SUM(gd.a_count + gd.ab_count + gd.b_count + gd.bc_count + gd.c_count + gd.d_count + gd.f_count) > 0
ORDER BY gpa_medio DESC
LIMIT 1;
```

- Qual é a disciplina mais difícil?

```{sql connection=conn}
SELECT 
    c.name AS nome_disciplina,
    co.course_uuid,
    SUM(gd.a_count*4.0 + gd.ab_count*3.5 + gd.b_count*3.0 + gd.bc_count*2.5 + gd.c_count*2.0 + gd.d_count*1.0) * 1.0 / 
    SUM(gd.a_count + gd.ab_count + gd.b_count + gd.bc_count + gd.c_count + gd.d_count + gd.f_count) AS gpa_medio
FROM course_offerings co
JOIN courses c ON co.course_uuid = c.uuid
JOIN subject_memberships sm ON co.uuid = sm.course_offering_uuid
JOIN subjects s ON sm.subject_code = s.code
JOIN grade_distributions gd ON co.uuid = gd.course_offering_uuid
WHERE s.abbreviation = 'STAT'
GROUP BY co.course_uuid
HAVING SUM(gd.a_count + gd.ab_count + gd.b_count + gd.bc_count + gd.c_count + gd.d_count + gd.f_count) > 0
ORDER BY gpa_medio ASC
LIMIT 1;
```

- Qual é a disciplina mais fácil?

```{sql connection=conn}
SELECT 
    c.name AS nome_disciplina,
    co.course_uuid,
    SUM(gd.a_count*4.0 + gd.ab_count*3.5 + gd.b_count*3.0 + gd.bc_count*2.5 + gd.c_count*2.0 + gd.d_count*1.0) * 1.0 / 
    SUM(gd.a_count + gd.ab_count + gd.b_count + gd.bc_count + gd.c_count + gd.d_count + gd.f_count) AS gpa_medio
FROM course_offerings co
JOIN courses c ON co.course_uuid = c.uuid
JOIN subject_memberships sm ON co.uuid = sm.course_offering_uuid
JOIN subjects s ON sm.subject_code = s.code
JOIN grade_distributions gd ON co.uuid = gd.course_offering_uuid
WHERE s.abbreviation = 'STAT'
GROUP BY co.course_uuid
HAVING SUM(gd.a_count + gd.ab_count + gd.b_count + gd.bc_count + gd.c_count + gd.d_count + gd.f_count) > 0
ORDER BY gpa_medio DESC
LIMIT 1;
```


4.  Desconecte do banco de dados.

```{r}
dbDisconnect(conn)
```

```{r}
Sys.setenv(TZ = "America/Sao_Paulo")
Sys.time()
```

